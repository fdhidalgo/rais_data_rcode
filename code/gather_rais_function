### gather_rais ###
## this function finds all files in a directory and creates a list of them

gather_rais <- function(base_dir, year){

states_list <- as.list(list.files(glue("{base_dir}/{year}")))

}
